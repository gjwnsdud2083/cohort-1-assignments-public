:80 {
    root * /usr/share/caddy
    file_server
    
    # JSON 파일에 대한 CORS 헤더 추가
    header {
        Access-Control-Allow-Origin *
        Access-Control-Allow-Methods "GET, POST, OPTIONS"
        Access-Control-Allow-Headers "Content-Type"
    }
}
